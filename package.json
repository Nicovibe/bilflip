{
  "name": "bilvipp",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "predev": "node scripts/copy-data.mjs && node scripts/fetch-images.mjs",
    "dev": "next dev",
    "prebuild": "node scripts/copy-data.mjs && node scripts/fetch-images.mjs",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "images:refresh": "node scripts/copy-data.mjs && node scripts/fetch-images.mjs --commit",
    "images:retry-empty": "node scripts/copy-data.mjs && node scripts/fetch-images.mjs --commit --retry-empty",
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@auth/drizzle-adapter": "^1.11.2",
    "@neondatabase/serverless": "^1.1.0",
    "drizzle-orm": "^0.45.2",
    "next": "^16.2.4",
    "next-auth": "^5.0.0-beta.31",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "resend": "^6.12.2",
    "stripe": "^22.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "dotenv": "^17.4.2",
    "drizzle-kit": "^0.31.10",
    "typescript": "^5.6.0"
  }
}
